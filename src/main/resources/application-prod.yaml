spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

management:
  tracing:
    sampling:
      probability: 0.1

cors:
  allowed-origins:
    - ${CORS_ALLOWED_ORIGINS:*}
